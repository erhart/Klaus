<h3>Listing doc_types</h3>

<table>
  <tr>
    <th>Id</th>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @doc_types.each do |doc_type| %>
  <tr>
    <td><%= doc_type.id %></td>
    <td><%= doc_type.name %></td>
    <td><%= link_to 'Show', doc_type %></td>
    <td><%= link_to 'Edit', edit_doc_type_path(doc_type) %></td>
    <td><%= link_to 'Destroy', doc_type, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Doc type', new_doc_type_path %>
