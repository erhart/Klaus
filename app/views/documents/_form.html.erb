<%= form_for(@document) do |f| %>
  <% if @document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@document.errors.count, "error") %> prohibited this document from being saved:</h2>

      <ul>
      <% @document.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= debugger %>
  <div class="field">
    <%= f.label :lesson %><br />
	<%= f.select :lesson_id, Lesson.all.collect { |p| [ p.name, p.id ] } %>
  </div>
  <div class="field">
    <%= f.label :semester %><br />
	<%= f.select :semester_id, Semester.all.collect { |p| [ p.name, p.id ] } %>
  </div>
  <div class="field">
    <%= f.label :prof %><br />
    <%= f.select :prof_id, Prof.all.collect { |p| [ p.name, p.id ] } %>
  </div>
  <div class="field">
    <%= f.label :doc_type %><br />
    <%= f.select :doc_type_id, DocType.all.collect { |p| [ p.name, p.id ] } %>
  </div>
  <div>
    <%= f.file_field :file %>
  </div>
  <div>
    <%= f.label :remote_file_url, "or file http://url" %><br />
    <%= f.text_field :remote_file_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
